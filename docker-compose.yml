version: '3.8'

services:
  diffbot-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: diffbot-mcp-server
    ports:
      - "${DIFFBOT_MCP_PORT}:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - MCP_TRANSPORT=http