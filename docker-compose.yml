version: '3.8'

services:
  diffbot-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: diffbot-mcp-server
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - MCP_TRANSPORT=http